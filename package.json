{
  "name": "novelforge-agent",
  "version": "0.10.0",
  "description": "Local-first long-form novel workflow engine for any MCP host (Claude Code, Codex CLI, …) or CLI. State machine + zod schemas + BM25 retrieval + persistent project state. No LLM dependency.",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "claude-code",
    "codex",
    "novel",
    "fiction",
    "writing",
    "agent",
    "workflow",
    "bm25",
    "retrieval"
  ],
  "homepage": "https://github.com/zlx362211854/novelforge-agent#readme",
  "bugs": {
    "url": "https://github.com/zlx362211854/novelforge-agent/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zlx362211854/novelforge-agent.git"
  },
  "license": "MIT",
  "author": "linkzhao <linkzhao8@gmail.com>",
  "type": "module",
  "bin": {
    "novelforge-agent": "./dist/src/cli/index.js",
    "novelforge-agent-mcp": "./dist/src/mcp/server.js"
  },
  "files": [
    "dist/src/**/*.js",
    "dist/src/**/*.d.ts",
    "src",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "test:e2e": "npm run build && bash scripts/e2e.sh",
    "inspect": "npm run build && npx -y @modelcontextprotocol/inspector node dist/src/mcp/server.js",
    "inspect:tools": "npm run build && npx -y @modelcontextprotocol/inspector --cli node dist/src/mcp/server.js --method tools/list",
    "inspect:prompts": "npm run build && npx -y @modelcontextprotocol/inspector --cli node dist/src/mcp/server.js --method prompts/list",
    "dev:mcp": "tsx src/mcp/server.ts",
    "dev:cli": "tsx src/cli/index.ts",
    "prepublishOnly": "npm run build && npm test",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "minisearch": "^7.2.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.19.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
